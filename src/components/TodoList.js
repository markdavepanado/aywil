import React from "react";

import "../styles/TodoList.css";

const TodoList = () => {
  return (
    <div className='todo-list'>
      <small>TodoList Panel</small>
    </div>
  );
};

export default TodoList;
